import{cmsFetch}from"../lib/cmsApi.js";
import React,{useEffect,useState} from "react";
import {ArrowRight,MapPin,Briefcase} from "lucide-react";
import Header from "../shared/Header.jsx";import Footer from "../shared/Footer.jsx";import FeedbackModal from "../components/FeedbackModal.jsx";
export default function CareersPage(){const [jobs,setJobs]=useState([]),[filter,setFilter]=useState('All'),[busy,setBusy]=useState(false),[modal,setModal]=useState({open:false});useEffect(()=>{cmsFetch('/api/jobs').then(r=>r.json()).then(setJobs).catch(()=>{})},[]);async function openApply(e){e.preventDefault();const form=e.currentTarget,fd=new FormData(form);setBusy(true);try{const r=await cmsFetch('/api/applications',{method:'POST',body:fd});if(!r.ok)throw new Error('Unable to submit your application.');form.reset();setModal({open:true,type:'success',title:'Application received.',message:'Thanks for your interest in Xynysys. We’ll review your profile and reach out if there’s a strong fit.'})}catch(err){setModal({open:true,type:'error',title:'Submission failed.',message:err.message})}finally{setBusy(false)}}const departments=['All',...new Set(jobs.map(x=>x.department).filter(Boolean))],shown=filter==='All'?jobs:jobs.filter(x=>x.department===filter);return <div className="corporate-page"><div className="page-dark"><Header/><div className="page-hero page-hero-wide"><div className="micro blue">CAREERS AT XYNYSYS</div><h1>Build what’s next<br/>with us.</h1><p>Join a technology consultancy focused on clarity, craftsmanship, deep engineering and meaningful business outcomes.</p></div></div><main className="career-index"><section><div className="index-heading"><span>OPEN ROLES</span><h2>Small teams.<br/><em>Serious work.</em></h2></div><div className="career-filters">{departments.map(x=><button className={filter===x?'active':''} onClick={()=>setFilter(x)} key={x}>{x}</button>)}</div><div className="career-role-list">{shown.length?shown.map(j=><a href={`/careers/${j.slug||j.id}`} key={j.id}><div><small>{j.department||'Technology'}</small><h3>{j.title}</h3><span><MapPin size={12}/>{j.location||'Hamilton / Hybrid'} <Briefcase size={12}/>{j.type||'Full-time'}</span></div><ArrowRight size={17}/></a>):<article className="empty-index"><h3>No published roles right now.</h3><p>You can still send an open application below.</p></article>}</div></section><aside className="career-open"><span>OPEN APPLICATION</span><h2>Exceptional people don’t always arrive on schedule.</h2><p>If your work is strong and you think you can contribute to Xynysys, introduce yourself.</p><form onSubmit={openApply} className="apply-form"><label>Full name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>LinkedIn / portfolio<input name="portfolio"/></label><label>What are you exceptional at?<textarea name="coverNote" rows="4"/></label><label>Résumé<input name="resume" type="file" accept=".pdf,.doc,.docx" required/></label><input className="form-honeypot" name="website" tabIndex="-1" autoComplete="off"/><button className="button dark" disabled={busy}>{busy?'Submitting…':<>Send open application <ArrowRight size={14}/></>}</button></form></aside></main><Footer/><FeedbackModal {...modal} onClose={()=>setModal({...modal,open:false})}/></div>}
