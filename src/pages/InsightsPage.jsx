import{cmsFetch}from"../lib/cmsApi.js";
import React,{useEffect,useState} from "react";import {ArrowRight,Clock} from "lucide-react";import Header from "../shared/Header.jsx";import Footer from "../shared/Footer.jsx";
export default function InsightsPage(){const [items,setItems]=useState([]),[filter,setFilter]=useState('All');useEffect(()=>{cmsFetch('/api/insights').then(r=>r.json()).then(setItems).catch(()=>{})},[]);const cats=['All',...new Set(items.map(x=>x.category).filter(Boolean))],shown=filter==='All'?items:items.filter(x=>x.category===filter);return <div className="corporate-page"><div className="page-dark"><Header/><div className="page-hero page-hero-wide"><div className="micro blue">INSIGHTS</div><h1>Useful thinking for<br/>serious technology decisions.</h1><p>Practical perspectives on software, AI, cloud, product engineering and the operating decisions around them.</p></div></div><main className="content-index"><div className="insight-filters">{cats.map(c=><button className={c===filter?'active':''} onClick={()=>setFilter(c)} key={c}>{c}</button>)}</div><div className="insight-grid">{shown.length?shown.map(x=><a href={`/insights/${x.slug}`} key={x.id}><span>{x.category||'ENGINEERING'}</span><h2>{x.title}</h2><p>{x.excerpt}</p><footer><small><Clock size={12}/>{x.readMinutes||5} min read</small><strong>Read insight <ArrowRight size={14}/></strong></footer></a>):<div className="empty-index"><h2>Insights are coming soon.</h2><p>Articles can be drafted, previewed and published from the CMS.</p></div>}</div></main><Footer/></div>}
