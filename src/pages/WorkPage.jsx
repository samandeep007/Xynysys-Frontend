import{cmsFetch}from"../lib/cmsApi.js";
import React,{useEffect,useState} from "react";import {ArrowRight} from "lucide-react";import Header from "../shared/Header.jsx";import Footer from "../shared/Footer.jsx";
export default function WorkPage(){const [items,setItems]=useState([]);useEffect(()=>{cmsFetch('/api/projects').then(r=>r.json()).then(setItems).catch(()=>{})},[]);return <div className="corporate-page"><div className="page-dark"><Header/><div className="page-hero page-hero-wide"><div className="micro blue">SELECTED WORK</div><h1>Systems designed to<br/>change how work gets done.</h1><p>Case studies focus on the business problem, the system behind the solution and what changed after delivery.</p></div></div><main className="content-index"><div className="work-index-grid">{items.length?items.map((x,i)=><a href={`/work/${x.slug||x.id}`} key={x.id}><div className="work-index-visual"><span>0{i+1}</span><div className="work-index-screen"><i/><i/><i/></div></div><div><small>{x.industry||'DIGITAL SYSTEM'} · {x.year||'Xynysys'}</small><h2>{x.title}</h2><p>{x.summary||x.description}</p><strong>Read case study <ArrowRight size={15}/></strong></div></a>):<div className="empty-index"><h2>Case studies are being prepared.</h2><p>Projects can be published from the Xynysys CMS when they are ready for public release.</p></div>}</div></main><Footer/></div>}
